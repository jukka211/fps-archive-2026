# Optional. Defaults live in src/sanity/env.ts (project rq2riu6f, dataset "production").
# Set these locally in .env.local, or in Vercel → Settings → Environment Variables, to override.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=
